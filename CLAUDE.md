# Veta for Claude Code

For installation, X bookmark sync, import, organization, and library queries,
read and follow `AGENT_INSTALL.md` completely.

Use Claude Code's current browser capability and existing subscription. Do not
ask the user to configure browser tooling, an X developer account, or an AI API
key. Register Veta with `npm run setup -- --agent claude`.
